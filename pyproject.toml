[project]
name = "repo-rebase"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "plotnine>=0.15.7",
    "polars>=1.42.1",
    "pyarrow>=24.0.0",
]
